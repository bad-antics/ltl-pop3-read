// examples/demo.ltl — runnable demo for ltl_pop3_read

import std::io
use ltl_pop3_read as lib

fn main() {
    io::println("ltl_pop3_read demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}